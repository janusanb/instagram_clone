// constants are just constants that are never changed.
export const USER_STATE_CHANGE = 'USER_STATE_CHANGE' // These are so things are very structured and not make mistakes when typing
export const USER_POSTS_STATE_CHANGE = 'USER_POSTS_STATE_CHANGE'